name: CI

on: 
    pull_request: 
    push: 
        branches: 
            - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "HELLO Guys from ci on Github"
