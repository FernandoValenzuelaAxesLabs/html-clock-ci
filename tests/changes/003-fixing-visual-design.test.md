# Test Specification: Fixing Visual Design

## New behavior

- There is not change of behavior, only background and Toggle position fixes.

## Regression requirements

- Existing clock update behavior continue to work.
- Existing markup required by current tests must remain available.
- Existing styles and layout must not be removed.
- All existing tests must continue to pass.
- All new tests codification must be added to the existing file named `tests\test.js`, in this case there was a previous background chang so maybe the test should be updated in that case.